# btop config
# color_theme "noctalia" relies on noctalia's community-templates generating
# a live theme at ~/.config/btop/themes/noctalia.theme at runtime.
# A static rose-pine theme ships alongside as a fallback (themes/rose-pine.theme).
color_theme = "noctalia"
theme_background = true
